# 3787 秒模拟场次参考资料审计

审计日期：2026-08-29  
审计方式：只读静态检查、`ffprobe`、SHA-256 校验、逐字稿时间轴解析、只读音频静音检测。未修改媒体、既有转录、业务代码、验收脚本、转录模块或分析模块。

## 一、结论

1. **媒体与 mvp-e 转录资料确属同一场次。** 实测媒体 SHA-256 为 `b6143bd6828af8ee801573f96db29b2e0faff0f8e99d2bdefe19ca5ec1e5bea9`，与 `metadata.json` 完全一致；媒体时长为 **3787.299122 秒**，音轨时长为 **3787.114500 秒**。
2. **既有资料中没有可审计的独立人工真值。** `transcript_raw.json` 明确由 `openai-whisper/base` 生成；`transcript_clean.md` 与原始稿保持相同 2238 个时间段，仅 5 行正文不同并增加 28 个“待确认”标记。目录中没有人工听写稿、标注者、复核记录、逐段判定或标注版本。`PASS_WITH_PENDING_MARKERS` 以及飞书 `readback_verified` 分别是自动质量/交付状态，均不等于人工准确率验证。
3. **既有机器稿可用作同源回归参考，不能用作“原文真值”计算准确率。** 新旧结果可计算“与 legacy Whisper 稿的一致率”，但必须明确标为 ASR-to-ASR agreement，不能称作抽样准确率。
4. **存在匹配该源媒体的既有自动分析，但只部分匹配且质量不足。** mvp-f 的场次键使用相同 `f4be143e...` 媒体摘要，N 场次、63.1 分钟及 00:00:12 开场原话均与该转录一致；然而 11 个主体模块全部为 0 秒/证据不足，缺少讲师确认、整场课程摘要和互动分析。报告还写“商品首次出现：N 未出现”，但逐字稿在 **00:52:02** 开始反复说明 5 元价格，并在 **00:56:53** 明确说“下面小黄车点开”，存在直接矛盾。因此该报告不能作为分析质量金标准。

## 二、参考资料及格式

### 源媒体

- 路径：`/Volumes/ExternalStorage/同行直播录制/continuous-monitor-recordings/抖音直播/陈兴笃学/陈兴笃学_2026-08-25_07-58-17.mp4`
- 容器/编码：MP4，H.264 视频，AAC 双声道 48 kHz 音频
- 媒体时长：3787.299122 秒（01:03:07.299）
- 音轨时长：3787.114500 秒（01:03:07.115）
- 文件大小：699,174,035 字节
- 实测 SHA-256：`b6143bd6828af8ee801573f96db29b2e0faff0f8e99d2bdefe19ca5ec1e5bea9`

### mvp-e 转录参考

目录：`/Volumes/ExternalStorage/同行直播录制/analysis/drafts/mvp-e/20260825-090221_陈兴笃学_f4be143e/`

| 文件 | 格式与用途 | 审计结果 |
|---|---|---|
| `metadata.json` | 来源哈希、媒体参数、ASR 配置、处理状态 | 来源与实测媒体匹配；记录转录耗时 688.595547 秒 |
| `audio_16k_mono.wav` | PCM s16le、16 kHz、单声道 | 3787.114688 秒，与源音轨相差约 0.000188 秒 |
| `transcript_raw.json` | 机器可读分段 | 2238 段；字段含 `start/end/text/avg_logprob/no_speech_prob/compression_ratio/low_confidence`；时间为小数秒 |
| `transcript_raw.md` | 带整秒时间戳的原始机器稿 | 2238 行分段正文 |
| `transcript_clean.md` | 清洗展示稿 | 相同 2238 个时间段；正文仅 5 行改变；28 行带“待确认”标记 |
| `feishu_delivery.json` | 飞书逐字稿交付状态 | 只证明文档创建和读回，不证明转录准确 |

原始 JSON 声明：引擎 `openai-whisper`，模型 `base`，语言 `zh`，低置信段 28，`cjk_ratio=0.8756621481`。元数据状态 `COMPLETE` 只表示流程完成，不能单独证明内容完整或准确。

## 三、既有稿时间覆盖证据

- 首段开始：0.00 秒
- 末段结束：3782.54 秒；末句为“我先下课了啊”
- 相对源音轨的首末跨度覆盖：**99.8792%**
- 末段至音轨结束：4.5745 秒；至容器结束：4.759122 秒
- 2238 段时间区间并集：3554.26 秒，占音轨 93.8514%。该值包含正常停顿，不应直接当作语音覆盖率。
- 只读静音检测（阈值 -45 dB、最短 0.3 秒）显示，末句结束附近从 3782.550833 秒起大部分为静音：3782.550833–3785.482083、3785.698125–3787.251500。中间约 0.216 秒活动可能是噪声或极短声音，需人工听辨。
- 同一阈值下，转录区间与“非静音活动”重叠率为 **97.1035%**，仍有约 103.969 秒活动不落在转录区间。该活动可能包含背景声、音乐、噪声或漏词，不能在未听审时等同为缺失语音。

因此，当前资料支持“时间轴基本延伸到自然结束语”的判断，但**不支持仅凭时间戳宣称完整转录了每一句话**。

较长的转录区间空档，建议纳入人工听审：

| 空档 | 长度 |
|---|---:|
| 00:32:12.82–00:32:20.86 | 8.04 秒 |
| 00:26:08.74–00:26:14.14 | 5.40 秒 |
| 00:41:51.74–00:41:55.94 | 4.20 秒 |
| 01:02:43.34–01:02:47.14 | 3.80 秒 |
| 00:11:11.58–00:11:14.50 | 2.92 秒 |

## 四、人工真值与可诚实报告的准确率口径

### 当前能报告

- **legacy 时间轴跨度覆盖率：99.8792%**，分母为源音轨时长，分子为首段到末段的跨度。
- **legacy 非静音活动重叠率：97.1035%（启发式）**，必须同时披露静音阈值与其并非语音真值。
- 新转录与既有稿之间可以报告：
  - 规范化字符一致率或 CER；
  - 分段时间对齐率；
  - 关键术语一致率。

上述结果只能命名为“与既有 Whisper base 参考稿的一致率”，不能命名为“抽样准确率”或“对原文准确率”。新链路如果也是同一模型或同一派生实现，结果高度一致也不是独立验证。

### 当前不能诚实报告

- 不能从 `low_confidence_count=28` 推导 98.75% 准确率；低置信标记率不是错误率。
- 不能从清洗稿仅改 5 行推导 99.78% 准确率；这只说明清洗改动少。
- 不能把 `PASS_WITH_PENDING_MARKERS`、`COMPLETE` 或飞书读回成功当作人工抽样通过。
- 没有人工听写真值时，不能给出对原音频的 CER/WER 或“抽样准确率”。

### 建议的人工抽样方案

建立独立人工真值后再计分。建议抽取 **12 个 30 秒窗口，共 360 秒，占音轨 9.51%**：

1. 均匀覆盖 8 段：00:00:00、00:07:30、00:15:00、00:22:30、00:30:00、00:37:30、00:45:00、00:52:30 起各 30 秒。
2. 风险补充 4 段：00:26:05、00:32:08（覆盖两处最大空档）、00:56:45（商品/小黄车）、01:02:37（收尾与尾段空档）起各 30 秒。
3. 人工逐字听写时保留数字、专名、学科组合、价格、CTA；标记重叠语音、听不清、背景声。疑难段至少二次复核。
4. 中文主指标采用去标点后的字符错误率 `CER=(替换+删除+插入)/人工真值字符数`，抽样准确率报告为 `1-CER`，按全样本字符数微平均；另报数字/专名/关键术语准确率。
5. 覆盖率另报“人工样本中有声语句完整出现的时长/语句数比例”和漏句清单，避免字符正确但整句缺失被掩盖。

## 五、既有分析与源媒体匹配性

相关自动分析目录：`/Volumes/ExternalStorage/同行直播录制/analysis/drafts/mvp-f/20260825-091425_陈兴笃学_f4be143e/`

### 匹配证据

- 目录名和 comparison manifest 的 N 摘要均为 `f4be143e30cff3ac60e18c813caa95622b15b5376b9d053f18e7d474ed0c1ccb`，对应本场媒体。
- 场次标识为 `douyin-71883486974-20260825-075817`，时长写 63.1 分钟，与源媒体一致。
- `analysis.json` 的 N 原话时间戳 00:00:12，引用“准备给孩子选文科……打个文……理科……打个礼……不知道……打零”，可在本场逐字稿同一时间找到。
- “文理 1 比 9”“六省份/两类孩子”“打省份/文理投票”的开场主题与前几分钟逐字稿相符。

### 不足与矛盾

- `opening_retention`、`parent_pain`、`demand_building`、`trust_building`、`fixed_explanation`、`knowledge`、`product_intro`、`product_bridge`、`conversion`、`qa`、`chitchat` 共 11 个模块均为 0 秒、无证据或等待人工复核。
- 报告没有确认讲师姓名；标题中的“陈兴笃学”只能证明账号/直播间标识，不能自动当作出镜讲师真实姓名。
- 报告没有整场课程内容摘要。逐字稿实际持续涉及文理方向、六省份规则、数学排名、重点/普通高中、物化生/物化地/物化政等选科组合。
- 互动只识别开场投票手法，没有系统覆盖打省份、打 36/38/70/111、关注/粉丝团、留言、购买引导等互动，也没有可核验的 UI/评论区统计。
- 报告写“商品首次出现：N 未出现”，与逐字稿 00:52:02 的“多少钱，5块钱”、00:56:53 的“下面小黄车点开”等直接冲突。
- `agent_judgment` 提到“留存至成交段人数可观”，但目录中没有观众留存曲线、平台后台数据或相应时间戳证据，不能作为已验证结论。

判定：**来源匹配，内容仅局部匹配，不能作为高质量分析报告的参考答案；最多可作为失败/退化基线。** 新分析报告应独立从本次转录和媒体证据生成，并逐项核对讲师身份（无法确认时明确写“待人工确认”）、课程结构、互动机制、商品与 CTA、关键原话及时间戳。

## 六、验收使用建议

- 转录覆盖：优先比较源音轨 3787.1145 秒与新转录首末时间轴，人工听审以上 12 个窗口及较长空档；不要仅看 `COMPLETE`。
- 转录准确：若本轮无人建立人工真值，报告“无法给出人工抽样准确率”，可附 legacy 一致率但不得改名。
- 分析质量：以源媒体、人工抽样和带时间戳的新转录逐条验真。既有 mvp-f 报告不应充当正确答案，尤其要检查商品/成交部分。
- 讲师字段：当前只可确认账号/竞品名为“陈兴笃学”；出镜讲师真实姓名仍需人工看画面或听取自我介绍确认。

