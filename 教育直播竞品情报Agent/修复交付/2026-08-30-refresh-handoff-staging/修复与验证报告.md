# 900秒刷新交接空档：最小修复与验证报告

日期：2026-08-30  
范围：提前解析下一条直播流地址；未引入双FFmpeg、HLS fallback、room ID拆场、finalizer、转录或分析改动。

## 结论

状态：**候选已原子安装，完整回归与压缩交接对比PASS；首个真实900秒刷新仍待自然到达。**

这次修复减少的是“当前FFmpeg结束后才开始解析下一地址”的空档。它不声称消除旧进程退出、ffprobe和新FFmpeg启动所需的全部时间。

## 修改

- 当前900秒段结束前30秒，用可取消daemon线程预解析下一条签名地址。
- 只有预解析结果仍LIVE且有流地址时才复用；失败则回退原按需解析。
- 任一时刻每个房间仍只有一个FFmpeg，避免重复内容和双倍连接。
- 预解析URL只保存在内存；sidecar/attempt继续只保存安全指纹，不保存原token。
- 当前段提前失败时取消预解析，不改变现有离线确认、失败次数或退避。

## 测试

- 旧实现确定性时序测试：EXPECTED_FAIL，证明下一地址原本只能在当前录制返回后解析。
- 新增正式专项测试2项，SharedRecorderTests由10项增至12项，两套环境均12/12 PASS。
- Profile Python完整回归：109/109 PASS。
- LaunchAgent服务Python完整回归：109/109 PASS。
- Python AST检查通过。

### 压缩交接对比

使用0.2秒录制周期和0.08秒模拟解析延迟，运行5轮：

| 指标 | 旧实现 | 新实现 |
|---|---:|---:|
| 中位交接空档 | 0.089824秒 | 0.000927秒 |
| 模拟解析空档减少 |  | 98.97% |

这是确定性压缩测试，不等同于真实平台秒数，也没有等待真实900秒。

## 生产安装与运行边界

- 安装前共享录制器SHA与隔离基线一致；采用同目录临时文件原子replace。
- 所有LaunchAgent PID保持不变；无需重载服务。
- 安装时已在运行的recorder继续使用内存旧代码，不受文件变化影响。
- 一场旧recorder随后自然退出；该进程启动于安装前，不可能执行预解析候选。Runtime已按原规则启动新PID 80568，新进程加载候选。
- 另一场旧recorder继续正常增长。
- Retention持续 `DELETE_DISABLED`。
- 抽查11个相关sidecar，未发现原始签名值。
- 新PID尚未走到首个900秒刷新，因此真实地址预解析与交接gap仍待自然证据；不能把压缩测试冒充真实实播验收。

## Trade-off

- 地址提前约30秒取得，极短TTL时可能使用前过期；失败会回退重新解析，但会保留一次空档。
- 每个活跃房间增加一个短时daemon线程和一次提前解析请求。
- ffprobe和新FFmpeg启动仍是串行，因此不是无缝录制。
- 双FFmpeg可进一步缩小空档，但会带来重复画面、带宽、限流和去重问题，本次不采用。

## 证据

- `candidate-preresolve.patch`
- `production-preresolve.patch`
- `生产部署前基线.json`
- `生产修改后哈希.json`
- `生产完整回归.json`
- `压缩交接对比.json`
- `生产部署后运行核验.json`（保留首次未通过观察）
- `生产第三项运行验证.json`（保留旧进程自然退出证据）
- `test-results/summary.json`
- 备份：`/Volumes/ExternalStorage/AgentInfrastructure/backups/edu-refresh-preresolve-fix-20260830/`
