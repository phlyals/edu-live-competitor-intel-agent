# 商品扫描与恢复链路：最小修复、自检记录

日期：2026-08-28，北京时间。仅限 `edu_live_competitor_intel`。
机器人：直播竞品情报主管，App ID `cli_a978a6e73f785cc5`。

## 修复范围

保留现有 Runtime、扫描顺序、数据库、表结构、浏览器后端与录制配置。
未重建系统，未更换其他 Agent 的配置，未添加表格开关。

- `bin/tabbit_scan.js`：切换筛选及翻页时等待同一商品、范围、页码对应的真实列表响应，并核对 DOM 总数、标题、同行名称和加载状态。没有硬编码 55/80 为生产期望值。
- `bin/tabbit_scanner.py`：识别结果集变化、页面渲染未完成、临时 HTTP 故障，交回既有有限重试机制。未知、身份或权限问题仍不放行。
- `v3_task_control.py`：授权范围内的定时重扫可被查询和恢复；“怎么样了”进入确定性状态查询；使用北京时间；不将已被成功扫描覆盖的历史失败当成当前任务。
- `v3_worker.py`：避免同一商品挂起时不断创建新定时任务；定时通知使用真实飞书消息来源；失败提示读取实际状态；完成后清除任务错误字段。
- `v3_scan_import.py`：重扫更新商品时保留既有元数据，包括历史商品的授权关联。
- `v3_project_feishu.py`：去除固定“55 条且身份已核验”的通用摘要；针对精确记录更新和只读请求，增加短暂网络断连的有限重试，不盲目重试结果不确定的创建请求。

## 已实际恢复的原任务

| 商品 | 原任务 | 本次扫描 | 观察条数 | 核验同行 |
|---|---|---|---:|---:|
| [有道领世]源哥知识兴趣读书卡 | task_9bcd6c597e2f25ce433b866a | scan:ab980f7b3515fd6f09b991d08ba768a1774a1cae | 55 | 20 |
| 初阶好家长诊断训练【爷爷奶奶勿拍】KCH 读书卡 | task_400fb3a249037315b2396aaf | scan:a913b41cc71e0341308099c26891f3d0b2dda88c | 80 | 21 |

两项均由原生产 Worker 领取原任务的第 2 次尝试完成，不是手动把状态改成成功。
恢复操作写入 `OPERATOR_TASK_RESUMED` 审计事件，未伪造飞书 Inbox 消息，未新建业务任务。

旧商品交付曾两次遭遇飞书 PATCH 网络 EOF；补充单记录安全重试后，第 3 次交付成功。新商品交付第 1 次成功。
两项最终 `COMPLETE / VERIFIED`，相应扫描交付与完成通知全部 `SENT`。

## 自检证据

- 最终代码在外置 Runtime Python 和服务 bootstrap Python 下，各执行 58 项测试，均通过。
- 实际延迟 1.5 秒的列表请求验证通过：读取 55 条正确结果，没有读取筛选切换前的临时值。
- 新商品真实页面范围验证读取 80 条，响应与 DOM 对齐。
- 两次完整生产扫描的浏览器回执无失败，均 `finished=true`，清理无 issue。
- 135 次同行临时页打开、135 次关闭；两个任务自建商品页也已关闭。
- 商品、同行账号、商品同行关系、账号监控四表按本次扫描和精确业务 ID 回读：两轮、两商品均通过（外置 Runtime Python 与服务 bootstrap Python 分别执行）。
- 链接字段按飞书实际 Markdown 链接返回格式提取目标 URL 后逐一比较，不忽略地址差异。
- 实际 PostgreSQL 验证：能准确选择定时任务，跨会话不能控制；两个当前商品任务均完成，普通“继续”不会重新启动已覆盖的历史任务。
- 22:09 两条完成通知已由正确 App 发出，并从飞书会话回读确认。
  - 旧商品消息：`om_x100b662d276034a0b1c3a6b2db57397`
  - 新商品消息：`om_x100b662d275648b0b29e9a7970785d8`

## 数据与范围边界

生产录制质量、视频/音频保留期限、其他 Agent 均未调整。原录制进程在服务更新期间继续运行。
本记录证明本次扫描—身份—保存—飞书交付及任务恢复故障已修复，不代表完成历史所有架构验收。
历史待核实占位和其他早期故障账本未删除；已被后续成功扫描覆盖的旧扫描尝试保留审计历史，不阻塞下一轮调度。

备份及自检脚本：`/Volumes/ExternalStorage/AgentInfrastructure/backups/edu-minimal-fix-20260828/`。
