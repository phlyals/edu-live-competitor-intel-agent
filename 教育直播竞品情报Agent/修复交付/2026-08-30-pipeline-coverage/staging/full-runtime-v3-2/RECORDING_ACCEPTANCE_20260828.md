# 录制与整场转录修复：实播验收待完成

## 已完成

- 仅修改现有 `bin/recorder.py`、`v3/v3_worker.py`、`v3/v3_pipeline_worker.py`。
- 修复只读探针 None 时长、空目录启动、媒体增长状态、连续失败四次后十分钟退避、样本与整场转录隔离、完整音频/ASR 时长校验。
- 历史 COMPLETE 标签需要实际覆盖证据，已知缺口不能用于完整实播验收。
- 两套运行环境各通过 72 项测试；回归测试中的媒体为模拟夹具，不算实播验收。
- 飞书入口、任务控制、商品扫描、同行发现、分析报告生成文件的 SHA256 与修复前相同；扫描/交付及 create_analysis_tasks 函数的 AST 未改变。
- 工作区 Git 交付补丁提交：935e022、8ce4733。生产 Profile 不在 Git 仓库内，因此补丁与测试提交在原有架构工作区仓库，补丁已反向检查与实际部署文件一致；没有初始化新生产仓库或提交凭证。
- 新 Runtime PID 85464、新 Pipeline PID 85437 于 2026-08-28 23:21 左右接手。原两路有效录制 PID 71640、81333 保留运行，没有因服务切换停止。
- 不带 duration 的真实只读探针已返回 OFFLINE_CONFIRMED，不再有参数 None 报错。

## 安全切换记录

旧服务在历史跨天媒体合并和旧转录中迟迟未退出。暂停旧父进程后确认合并子进程已结束，将尚未验证的合并产物改名为 `整场直播.pre-reload-unverified.ts.audit` 保留，原片段保留供新代码重新处理。停止旧的未完成 ASR 尝试，原始媒体和约 48 分钟的音频仍在。没有删除原始媒体来促成验收。

## 尚未完成，禁止报告为验收通过

需要至少一个在新服务重载之后（2026-08-28T15:21:37Z）新开播的真实同行场次：

1. 从真实开播检测开始录制，文件持续增长。
2. 自然下播并完成五分钟确认，不伪造开播/下播或人为截短。
3. 录像可播放、没有已知断档，媒体时长与观察到的场次时间相符。
4. 生成覆盖全音频的 FULL_SESSION 转录，不使用旧五分钟样本。
5. 现有 DeepSeek 分析生成完成，人工式阅读确认可用；分析生成逻辑不得修改。
6. 亲自打开播放媒体、阅读全稿并对照音频检查，保存真实验收结论。没有做过的检查不得声称通过。

本次运行时仍无符合新开播时间条件的已完成场次，因此不交付“整场验收通过”的结论。
已建立当前任务每十分钟续检：automation-3（真实整场直播录制转录验收）。全部条件通过后交付媒体、逐字稿、分析报告及核验记录，并暂停该续检。

历史跨天拼接文件、带重启缺口的录像、旧的 48 分钟音频均不作为本次新完整场次的替代。

## 23:38 续检：媒体登记迁移冲突

发现临时目录移到完成目录后，recording_segments 沿用同一 segment_id，但原 SQL 只处理 (session_id,path) 冲突；旧路径行导致主键冲突，Runtime 和 Pipeline 反复退出。
已在原 v3_worker.py、v3_pipeline_worker.py 修复：优先复用已有媒体编号，按原编号更新迁移路径；对两个仍存在且内容不同的路径拒绝覆盖。新增四项回归，两套环境各 76 项通过。
补丁提交：4760179（仍为原三个文件的累计交付补丁，无红线文件改动）。实际冲突编号 segment_6b10e2fb57a943a70e5b634b 已保留并指向 completed 目录。
检查时 Pipeline PID 91389 已 READY；Runtime PID 91289 正在历史媒体收尾，尚为 STARTING；两份错误日志在 23:35:42 后未新增该冲突。原有效录制 PID 71640、81333 仍存活。修复重载后新增场次仍为 0，真实整场验收继续待完成。
