# 商品工作流修复与实测结果 · 2026-08-28

## 本次范围与结果

修复：继续/状态控制、近30天验证、同行身份闭环及临时页清理、扫描编号冲突。

原任务 `task_f3f2e806c9514e65a4c8153a93683a9a` 已恢复为 `COMPLETE / VERIFIED`。
商品 `douyin:3838016038189006849` 本次页面提供80条直播记录，已完整遍历7页。
46个Buyin别名通过可见二维码归并为21个抖音用户；21个监控地址均实际核验身份，已进入调度并产生检测时间。

扫描版本：`scan:807ee8953e09993c767777a279da0bb580cc58c6`。
结果真本：`/Volumes/ExternalStorage/同行直播录制/analysis/drafts/runtime-v3/task_f3f2e806c9514e65a4c8153a93683a9a/tabbit-e2e-20260828/result.json`。

## 修改与验证

- 新增 `v3_task_control.py`：按当前用户/会话处理继续、恢复、状态；不进入通用LLM，不生成新业务任务；重复控制消息幂等；恢复保留历史重试和租约代次。
- 新增 `tabbit_scanner.py` / `tabbit_scan.js`：仅使用稳定Tabbit CLI任务工作区，验证近7天/近30天互斥选中属性、商品ID、直播标签、每页总数和末页；不依赖旧的记录年龄推断。
- 详情页点击取得Buyin UID与可见二维码，逐个处理，`finally`关闭任务临时页。80次开页、80次关页；最终保留用户原有商品页及其他标签。
- 二维码解码与站内重定向取得稳定抖音用户ID；真实探针核验监控地址。UNKNOWN或身份不匹配不得作为完成绑定。
- 新增 `v3_scan_import.py`：完整性与身份校验后单事务入库；名称不是主键；商品、任务、结果摘要共同生成扫描ID。
- 每次尝试使用独立目录。已提交的扫描通过摘要和Checkpoint恢复到交付，不重复浏览器扫描。
- 飞书投影按商品限定范围，不再只写最近一个商品；完成状态限定到对应任务，完成通知走Outbox。
- 已更新Agent旧操作说明，移除“短链不能自动接单、需要用户重发”的过期规则。

## 两轮检查

- 生产启动Python环境：42项单元/回归测试通过。
- 外置盘Python环境：相同42项再次通过。
- 覆盖100次并发控制/消息幂等、跨会话拒绝、重复分享选择原任务、同名不同用户不合并、冲突事务回滚、同文件名多商品不冲突、提交后恢复不打开浏览器。
- 原始“继续处理”飞书消息已通过真实Gateway解释器处理并回读：返回原任务已完成，不重复扫描。
- 飞书回读：2个商品的唯一键均保留；新商品21个账号、21条关系、21个监控对象唯一键一致。
- PostgreSQL复核：新扫描80条观察均为VERIFIED_UID；21条关系引用新扫描版本；结果文件SHA256与数据库一致。
- 旧商品55条历史扫描保留在旧版本，不被新商品覆盖。

## 边界

本次结论针对上述四项修复及这个商品任务，不代表整个Runtime的所有历史发布门槛通过。
旧商品在本次修复前已存在20条名称占位的待核实记录；本次没有按名字擅自合并或删除它们。
ASR人工金标、独立远端容量证明等此前未完成的发布条件仍需独立验收。
监控绑定时核验成功不代表以后始终开播或网络始终正常；后续UNKNOWN保留为UNKNOWN。
