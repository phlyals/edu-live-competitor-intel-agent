--- /Users/mac/.hermes/profiles/edu_live_competitor_intel/runtime/v3/v3_runtime.py	2026-08-30 21:53:21
+++ /Users/mac/Documents/agent架构师/教育直播竞品情报Agent/修复交付/2026-08-30-long-job-claim-retry/staging/runtime/v3/v3_runtime.py	2026-08-30 22:57:50
@@ -155,13 +155,29 @@
             _ensure_column(conn, "tasks", "next_attempt_at", "TEXT")
             _ensure_column(conn, "outbox", "max_attempts", f"INTEGER NOT NULL DEFAULT {OUTBOX_MAX_ATTEMPTS}")
             _ensure_column(conn, "transcripts", "language", "TEXT")
+            for table in ("transcripts", "analyses"):
+                _ensure_column(conn, table, "updated_at", "TEXT")
+                _ensure_column(conn, table, "attempts", "INTEGER NOT NULL DEFAULT 0")
+                _ensure_column(conn, table, "max_attempts", "INTEGER NOT NULL DEFAULT 5")
+                _ensure_column(conn, table, "next_attempt_at", "TEXT")
+                _ensure_column(conn, table, "last_attempt_at", "TEXT")
+                _ensure_column(conn, table, "lease_owner", "TEXT")
+                _ensure_column(conn, table, "lease_until", "TEXT")
+                _ensure_column(conn, table, "lease_epoch", "INTEGER NOT NULL DEFAULT 0")
+                _ensure_column(conn, table, "last_error_type", "TEXT")
+                _ensure_column(conn, table, "last_error", "TEXT")
+                _ensure_column(conn, table, "checkpoint_json", "TEXT NOT NULL DEFAULT '{}'")
             # Fail closed on upgraded databases.  The explicit lifecycle
             # migration classifies legacy rows before the pipeline is resumed.
             _ensure_column(conn, "recording_segments", "lifecycle_status", "TEXT NOT NULL DEFAULT 'UNCLASSIFIED'")
             _ensure_column(conn, "recording_segments", "superseded_by_segment_id", "TEXT REFERENCES recording_segments(segment_id)")
             _ensure_column(conn, "recording_segments", "lifecycle_updated_at", "TEXT")
             conn.execute("CREATE INDEX IF NOT EXISTS idx_recording_segments_lifecycle ON recording_segments(lifecycle_status,session_id)")
+            conn.execute("CREATE INDEX IF NOT EXISTS idx_transcripts_long_job_due ON transcripts(status,next_attempt_at,lease_until)")
+            conn.execute("CREATE INDEX IF NOT EXISTS idx_analyses_long_job_due ON analyses(status,next_attempt_at,lease_until)")
             conn.execute("UPDATE tasks SET next_attempt_at=COALESCE(next_attempt_at,updated_at) WHERE status IN ('RECEIVED','RETRY_WAIT')")
+            conn.execute("UPDATE transcripts SET updated_at=COALESCE(updated_at,created_at),next_attempt_at=CASE WHEN status IN ('PENDING','WAITING_TOOL','PAUSED','RETRY_WAIT') THEN COALESCE(next_attempt_at,created_at) ELSE next_attempt_at END")
+            conn.execute("UPDATE analyses SET updated_at=COALESCE(updated_at,?),next_attempt_at=CASE WHEN status IN ('PENDING','WAITING_MODEL','RETRY_WAIT') THEN COALESCE(next_attempt_at,?) ELSE next_attempt_at END", (utc_now(), utc_now()))
             conn.execute("CREATE INDEX IF NOT EXISTS idx_tasks_due ON tasks(status,next_attempt_at,lease_until)")
             conn.execute("INSERT INTO schema_meta(key,value) VALUES('control_plane_revision','3.1') ON CONFLICT(key) DO UPDATE SET value=excluded.value,updated_at=strftime('%Y-%m-%dT%H:%M:%fZ','now')")
             version = conn.execute("SELECT value FROM schema_meta WHERE key='schema_version'").fetchone()
