# 真实直播流刷新模拟验证报告

日期：2026-08-29

## 输入与隔离边界

用户指定的 `回放测试/3787s_replay.flv` 不存在，因此按“或实际回放文件路径”使用已知完整媒体：

`/Volumes/ExternalStorage/同行直播录制/continuous-monitor-recordings/抖音直播/陈兴笃学/陈兴笃学_2026-08-25_07-58-17.mp4`

源时长 `3787.299122` 秒。测试只绑定 `127.0.0.1`，没有访问直播平台，也没有调用生产数据库、录制任务、转录或分析。为压缩墙钟时间，将完整回放按媒体时间切为 600、600、900、900、787.334 秒五个独立 FLV 源段；300 秒与 180 秒中断分别压缩为 3.0 秒和 1.8 秒墙钟等待。媒体内容缓存在本地，所以恢复后可以从断点继续；真实无 DVR 的直播不能据此宣称可回补断流期间内容。

## 场景与关键日志

| 步骤 | 媒体时间行为 | 证据 |
|---:|---|---|
| 1 | 录制首个 600.034 秒 FLV | `SOURCE_SEGMENT_FINISHED source_index=0` |
| 2 | 虚拟断流 300 秒 | `VIRTUAL_STREAM_INTERRUPTION virtual_gap_seconds=300`，3 秒后恢复 |
| 3 | 继续录制 600.034 秒 | URL 重新解析且 SHA-256 从 `741d3a5d0301354c6d9b8412ec49c084cc1c0b949ce7abd41d554f95483a3d6d` 变为 `5f015646fe2e898c18fbbaeb83e3c8c5d3f32480253ec4df3f05c0dbe19e0cfa` |
| 4 | 虚拟断流 180 秒 | `VIRTUAL_STREAM_INTERRUPTION virtual_gap_seconds=180`，1.8 秒后恢复 |
| 5 | 继续到结尾 | 后续片段约 900.034、900.034、787.334 秒；最终三次离线观测后结束 |

完整关键行见 [run-002/关键日志.md](./run-002/关键日志.md)，原始 NDJSON 见 `run-002/simulation-events.jsonl`。

## 要求核验

| 要求 | 结果 | 证据 |
|---|---|---|
| 约 900 秒触发地址刷新 | PASS | 健康流两个完整周期分别为 900.033667、900.022667 秒，下一地址解析在约 0.24、0.13 秒后发生；断流则不等满 900 秒，提前重新解析 |
| 刷新后 `url_sha256` 变化 | PASS | 5 个录制片段共有 5 个唯一 SHA-256 |
| 中断次数少于 3 时继续 | PASS | 前两段提前结束形成连续失败计数 1、2；第三段完成 900 秒后计数归零，进程未结束 |
| 恢复后保持同一场次 | PASS | `business_session_count=1`，`process_restart_count=0` |
| 追加到最终同一媒体 | PASS（业务语义）；非原文件字节追加 | 不新建业务场次；内部保留 5 个可审计刷新片段，最终无转码合并为 `/Volumes/ExternalStorage/agent架构师/教育直播竞品情报Agent/修复交付/2026-08-29-流刷新模拟验证/run-002/整场直播.ts` |
| 最终时长接近 3787 秒 | PASS | 输出 `3787.423333` 秒，差 `0.124211` 秒，相对误差 `0.00327967%` |
| 是否有录制缺口 | 无（本次缓冲模拟） | `recording_gap_seen=false`；所有五段累计并合并 |
| 原始 token 是否泄漏 | PASS | sidecar 仅含 query key 和哈希，`raw_tokens_absent_from_sidecars=true` |
| 原验收脚本 | PASS | `run-002/acceptance/simulation_3787s_two_interruptions_check_recording.json` |

## 结果

- 最终录制时长：**3787.423333 秒**
- 进程重启次数：**0**
- 内部地址刷新/媒体片段数：**5**
- 业务场次数：**1**
- 录制缺口：**否（缓冲回放模拟口径）**
- 墙钟运行耗时：**71.957 秒**
- 总体状态：**PASS**

`run-001` 因本地夹具未在 900 秒客户端主动关闭后推进源段而人工停止，已标记 ABORTED，未产生 summary，不作为结果。`run-002` 修复了测试夹具状态推进；未修改生产录制代码。
