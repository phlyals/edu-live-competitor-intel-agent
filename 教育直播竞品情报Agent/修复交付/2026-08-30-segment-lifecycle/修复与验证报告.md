# Segment生命周期与失效转录治理：修复与验证报告

日期：2026-08-30

## 结论

状态：**生产迁移、代码部署、完整回归和数据核验PASS。** 历史行未删除，245条永久失败转录只退出待处理队列。

## 安全准备

- 迁移前`pg_dump -Fc`成功，`pg_restore --list` 215项，dump SHA已保存、权限0600。
- 另保存逐行segment/transcript JSON备份，权限0600。
- 只读dry-run在冻结Runtime/Pipeline后生成计划SHA；apply必须匹配数据库名和计划SHA，否则整笔拒绝。
- 数据迁移单事务、advisory lock、5秒lock timeout；重复apply返回`ALREADY_APPLIED`。

## 修改

- recording_segments新增`lifecycle_status`、`superseded_by_segment_id`、`lifecycle_updated_at`。
- 状态：`CANONICAL_ACTIVE`、`SOURCE_RETAINED`、`SOURCE_SUPERSEDED`、`LOST_REVIEW`、`UNCLASSIFIED`。
- 新表默认`UNCLASSIFIED`，迁移完成前pipeline fail-closed。
- Pipeline只选择`COMPLETE + CANONICAL_ACTIVE`进入转录。
- Finalizer验证后才标canonical；保留源更新到新completed路径，冲突时不覆盖。
- 只有严格匹配segment引用、source_path、失败原因、无output且source已superseded的WAITING_TOOL才改为`CANCELLED_SUPERSEDED_SOURCE`。

## 生产迁移结果

| 状态 | 数量 |
|---|---:|
| CANONICAL_ACTIVE | 99 |
| SOURCE_RETAINED | 2 |
| SOURCE_SUPERSEDED | 274 |
| LOST_REVIEW | 0 |
| UNCLASSIFIED | 0 |

- canonical路径存在99/99。
- superseded路径缺失274/274。
- retained路径存在2/2。
- WAITING_TOOL：245→0。
- CANCELLED_SUPERSEDED_SOURCE：245。
- COMPLETE transcript：6，未被迁移修改。
- migration version：1。

## 测试与部署

- SQLite规划测试两套环境各3/3 PASS。
- 候选代码测试两套环境各4/4 PASS。
- 临时PostgreSQL：dry-run只读、错误计划拒绝、apply、重复apply、DDL幂等、0600备份全部PASS。
- 完整Runtime回归两套环境各131/131 PASS。
- Runtime PID 90013→16001、Pipeline PID 91389→15999，正常退出码0。
- 暂停期间6个recorder子进程继续运行；恢复后多场继续增长。
- Retention持续`DELETE_DISABLED`。

## 完整媒体哈希边界

- 低优先级重算前30个canonical、读取51.67GB，30/30 SHA匹配。
- 持续全盘读取开始拖慢隔离测试，为不影响生产录制主动停止。
- 正式迁移使用manifest文件SHA、final exact path/bytes、manifest SHA与canonical segment checksum三重交叉验证；无异常、计划issues=0。

## 证据

- `数据库迁移前备份.json`
- `生产dry-run.md/.json`
- `生产迁移dry-run-frozen.json`
- `生产迁移apply.json`
- `生产迁移重复apply.json`
- `生产迁移后验证.json`
- `production-segment-lifecycle.patch`
- `生产完整回归.json`
- 备份目录：`/Volumes/ExternalStorage/AgentInfrastructure/backups/edu-segment-lifecycle-20260830/`
