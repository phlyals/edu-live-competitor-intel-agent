# Validation artifacts

- `simulation_3787s.analysis.v3.json` is the final accepted artifact.
- `simulation_3787s.analysis.json` and `simulation_3787s.analysis.final.json` are intermediate successful runs made before the final exact-segment timestamp projection. They are retained for audit and must not be treated as the final acceptance result.
