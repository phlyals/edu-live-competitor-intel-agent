# edu_live_competitor_intel 录制/监控全链路只读专项审计

审计时间：2026-08-30 00:21 CST（2026-08-29T16:21Z）  
边界：只读检查代码、配置、PostgreSQL、进程、媒体、sidecar 与日志；数据库事务显式使用 `BEGIN READ ONLY`。未运行 `init_db`，未修改代码、配置、数据库或服务，未启动录制。

审计版本 SHA-256：

- `runtime/bin/recorder.py`：`db6ed793438b72cd9be47a8b82f31e172da94cd03365df079f160c1d44ea0ec6`
- `runtime/v3/v3_worker.py`：`68d4b0f3accad3bf2c2ed78fd688ad593c0e01cd1072d5baa23b6ff25dfa4460`
- `runtime/bin/streamget_probe.py`：`3b070001c651255b74bdcde4ff742a331b40376195a55920a257b34c82dc5b47`
- 共享 `record_douyin_live.py`：`d215c0b527350304e79d6b18f886b43fec320d18e7577710a6fddfd0b7b74ba9`

## 总结

本轮 FLV 优先、900 秒刷新、唯一分段和安全指纹已经真实生效，但链路仍不能按“`restart_count=0` 即完整”验收。最严重的问题是**一次最终文件增长会被无限重复当成新增长，从而永久否决下播**；这已经导致场次不结束，并曾把相隔约 4 小时的两段采集合并进同一业务场次。完整性规则、原验收脚本和 finalizer 也没有消费 sidecar 的交接盲区及 FFmpeg 严重尾日志。

截至快照：41 个活跃监控目标；部署阈值后共有 22 个新场次，其中 20 个 `MEDIA_COMPLETE/PARTIAL`、2 个 `WAITING_STREAM/UNKNOWN`，没有一个新场次为 `COMPLETE`。5 个已结束且 `restart_count=0` 的场次也全部为 `PARTIAL`。

## P0：下播增长保护形成永久锁存，并可把不同直播拼成同一场

### 代码证据

- [`v3_worker.py:189`](/Users/mac/.hermes/profiles/edu_live_competitor_intel/runtime/v3/v3_worker.py:189) 至 198：只要目录当前字节数大于 `recording_health.bytes` 就否决离线，但否决后**不把基线更新为当前字节数**。
- [`v3_worker.py:555`](/Users/mac/.hermes/profiles/edu_live_competitor_intel/runtime/v3/v3_worker.py:555) 至 573：recorder 已死且目标为 `OFFLINE_CONFIRMED` 时，不再调用 `recording_progress`，因此该字节基线不会更新。
- [`v3_worker.py:123`](/Users/mac/.hermes/profiles/edu_live_competitor_intel/runtime/v3/v3_worker.py:123) 至 136：存在未结束场次时，下一次 LIVE 继续返回旧场次；若 room ID 变化，还会修改旧场次的 `platform_session_id`，而不是创建新场次。

### 生产证据

- `session_1f8cf7964e97294d607f961af139d550`（高考通）：recorder PID 为空、job 为 `WAITING_STREAM`、目标为 `OFFLINE_CONFIRMED`；健康基线 708,094,416 字节，实际固定为 708,144,864 字节，恒差 50,448 字节。相同最终增长已触发 61 次 `OFFLINE_REJECTED_MEDIA_GROWTH`，`ended_at` 仍为空。
- `session_6ca51b8271186de3ee597afd093706c1`（方老师讲高中升学）：同样已离线且 PID 为空；健康基线 1,634,913,804 字节，实际 1,635,090,896 字节，恒差 177,092 字节，已重复拒绝 4 次。
- `session_1dce6f824cc0dccc43e4d4bda99ecae8`（高考通）sidecar 显示第一段于 07:59:36Z 结束，下一段至 12:02:03Z 才开始，中间 **14,546.810 秒**无录制进程；finalizer 后两段仍成为同一个 `整场直播.ts`。manifest 却把 `captured_from/to` 写成 07:04:17Z–12:24:56Z。
- 近期 2,941 条 domain event 中，2,816 条是 `OFFLINE_REJECTED_MEDIA_GROWTH`，与该锁存一致。

### 影响

场次无法自然结束、finalizer/转录被阻塞；主播再次开播时可能续写旧场次，跨场拼接；业务时间与真实采集区间不一致。

### 建议

增长否决必须“消费”当前增长：原子保存本次实际字节、文件 mtime 和 sidecar generation，只允许每次真实新增长否决一次。只有 recorder/FFmpeg 仍存活且 mtime 足够新时才允许否决离线。room ID 变化时保留旧场次和 room 历史，结合最后 LIVE、首次 OFFLINE、进程/sidecar 状态结束旧场并创建新场；禁止覆盖原 metadata。

## P1：完整性规则可忽略已知盲区和严重 FFmpeg 错误

### 代码证据

- [`v3_worker.py:490`](/Users/mac/.hermes/profiles/edu_live_competitor_intel/runtime/v3/v3_worker.py:490) 至 497：finalizer 只探测容器时长；`COMPLETE` 容差为 `max(60 秒, 场次时长 2%)`，不读 sidecar 交接间隔、stderr、流数量、包连续性或 `recording_gaps`。
- [`check_recording.py:19`](/Users/mac/Documents/agent架构师/教育直播竞品情报Agent/验收脚本/check_recording.py:19) 至 24：原验收脚本只检查文件可读和时长误差不超过 5%，不检查 `completeness`、`recording_gap_seen`、restart、sidecar 或 FFmpeg 错误。
- [`record_douyin_live.py:196`](/Volumes/ExternalStorage/AgentInfrastructure/isolated/shared/avtranscribe/record_douyin_live.py:196) 至 201 对音视频均使用可选 map；因此无音频或无视频也可生成文件。finalizer只查 format duration。

### 生产证据

5 个已结束且 `restart_count=0` 的场次均为 `PARTIAL`，媒体时长/观测时长分别为：

| 场次 | 比例 | `recording_gap_seen` |
|---|---:|---|
| 白老师讲升学 `session_d248...` | 93.66% | true |
| 班主任罗老师 `session_c172...` | 95.69% | 无 |
| 名师出高徒 `session_2f3e...` | 95.35% | 无 |
| 升学未来 `session_7cfe...` | 94.75% | 无 |
| 崔老师升学思维论 `session_5837...` | 86.25% | 无 |

其中 95.69% 和 95.35% 按原 5% 规则会通过，即使 Runtime 自己判为不连续。初始固定快照的 126 个近期 sidecar 中，有 26 个同时满足 `return_code=0` 且尾日志含 404、premature end、demux I/O、invalid data 或 packet mismatch；其中至少 7 个之后仍有后续刷新段，不都能解释为自然下播。

### 建议

完整性应由 sidecar 构造真实采集区间并求并集：要求起点/终点误差、每个交接 gap、进程退出类型、严重 stderr 分类、音视频流存在、PTS/DTS 连续性和最终媒体可完整读取全部通过。时长比例只能作为一项证据；长场次不能允许 2% 或 5% 的线性放大缺失。

## P1：900 秒刷新本身存在未记录的交接盲区

### 代码证据

- [`record_douyin_live.py:337`](/Volumes/ExternalStorage/AgentInfrastructure/isolated/shared/avtranscribe/record_douyin_live.py:337) 录完当前 900 秒段后，才返回 Python 做 ffprobe、重新解析 URL，再启动下一 FFmpeg；没有预解析或重叠交接。
- [`record_douyin_live.py:343`](/Volumes/ExternalStorage/AgentInfrastructure/isolated/shared/avtranscribe/record_douyin_live.py:343) 的 attempt 记录不含 `resolved_at`；该时间只存在各 sidecar。

### 生产证据

5 个无进程重启的已结束场次共有 20 个相邻 sidecar 交接盲区，合计 **57.647 秒**，中位 1.868 秒，最大 8.949 秒。它们没有进入 `recording_gaps`，也不会设置 `recording_gap_seen`。

### 建议

在当前段结束前并行预解析下一 URL，并使用短重叠录制后按时间戳/内容去重，或采用保持单一采集进程的可刷新输入代理。至少把 `previous.ended_at → next.started_at` 写成 durable gap，超阈值即不得 COMPLETE。

## P1：finalizer 在证据不足时删除源分段，且未验证拼接连续性

### 代码证据

- [`v3_worker.py:445`](/Users/mac/.hermes/profiles/edu_live_competitor_intel/runtime/v3/v3_worker.py:445) 至 450 仅按文件 mtime 排序，未使用 sidecar `started_at` 或逻辑序号。
- [`v3_worker.py:460`](/Users/mac/.hermes/profiles/edu_live_competitor_intel/runtime/v3/v3_worker.py:460) 至 469 只要 concat 返回 0 且输出非空，就发布最终文件并立即删除全部源媒体分段。
- [`v3_worker.py:503`](/Users/mac/.hermes/profiles/edu_live_competitor_intel/runtime/v3/v3_worker.py:503) 至 507 的 manifest 只记录最终文件，`segment_count` 固定为 1，不保存源分段顺序、哈希、时长、sidecar、退出码和交接 gap。

### 生产证据

- 对 `session_2f3e...` 最终 TS 做只读全包 copy 检查，仍出现非单调 DTS 警告。
- 固定快照中，115 个已归档 sidecar 的 `output_path` 仍指向已移动且不存在的 partial 路径。
- 新场次最终目录只保留一个媒体文件；源刷新媒体已删除，无法在事后逐段重跑解码或证明 concat 未丢包。
- 数据库有 58 个 `VERIFIED` manifest 对应 `PARTIAL` 场次，303 条 `recording_segments.status=COMPLETE` 对应 `PARTIAL` 场次；状态语义容易被下游误用。

### 建议

先生成不可变源分段清单（序号、真实开始/结束、大小、SHA、stream fingerprint、退出与错误分类），拼接后验证最终时长≈有效源段时长之和、音视频流、全包读取、时间戳单调和 gap，再原子发布。源分段应保留至验证成功/冷却期后；sidecar 不必改写，但 manifest 要提供旧路径到归档位置的映射。`VERIFIED`/segment `COMPLETE` 与场次覆盖状态必须拆成不会混淆的字段。

## P1：单个 `ps`/finalizer 异常可杀死整个监控守护进程

### 代码证据

- [`v3_worker.py:266`](/Users/mac/.hermes/profiles/edu_live_competitor_intel/runtime/v3/v3_worker.py:266) 和 [`v3_worker.py:278`](/Users/mac/.hermes/profiles/edu_live_competitor_intel/runtime/v3/v3_worker.py:278) 对每个 job 重复执行全量 `ps -axo pid,command`；超时没有捕获。
- [`v3_worker.py:521`](/Users/mac/.hermes/profiles/edu_live_competitor_intel/runtime/v3/v3_worker.py:521) 每轮查询全部历史 recording job，没有过滤终态；快照 heartbeat 每轮把 68 个旧 job 计为 `finalized`。
- [`v3_worker.py:985`](/Users/mac/.hermes/profiles/edu_live_competitor_intel/runtime/v3/v3_worker.py:985) 的 daemon 主循环只捕获 KeyboardInterrupt，没有每 job 或每轮异常隔离。

### 生产证据

`runtime-v3.error.log` 有 56 个 traceback：30 次未捕获的全量 `ps` 10 秒超时，另有 26 次旧版本 finalizer 主键冲突。bootstrap 记录 61 次启动；launchd 当前显示历史 `last exit code=1`。当前代码已增加 segment 冲突保护，但 `ps` 超时和全局异常扩散路径仍存在。runtime stdout 日志为空，heartbeat 仍可在当前进程恢复后显示 READY。

### 建议

每轮只取一次进程快照并建立 PID/recording_key 映射；超时返回 UNKNOWN，不能抛出到 daemon。只 reconcile 非终态/待 finalization job；每场异常单独事件化并继续其他目标。concat、ffprobe、hash、filesystem 和 DB 冲突都需每场边界。heartbeat 指标只统计本轮真实状态迁移，不重复把历史终态算作 finalized。

## P1：流地址只“声明”HLS fallback，实际没有失败切换

### 代码证据

- [`record_douyin_live.py:100`](/Volumes/ExternalStorage/AgentInfrastructure/isolated/shared/avtranscribe/record_douyin_live.py:100) 至 110 虽保存 FLV/HLS candidates，却只把第一个 FLV 放进 `stream_url`。
- [`record_douyin_live.py:337`](/Volumes/ExternalStorage/AgentInfrastructure/isolated/shared/avtranscribe/record_douyin_live.py:337) 每次只调用该单一 URL；`stream_urls` 从未循环消费。
- 上游 `stream.py:65` 只用 HLS 检查质量可达性，然后本包装器选择未单独探测的 FLV。

生产快照中所有 132 个近期 sidecar 都是 FLV；即使 FLV 出现 404/I/O，也没有一次 HLS fallback 证据。

### 建议

保持 FLV 首选，但对“新解析的 FLV”失败做明确分类；在仍确认 LIVE 时，可对同一次新解析得到的 HLS 做一次受限、可观测的 fallback。不得恢复旧 URL 的无限 EOF 重连。

## P2：状态与故障证据仍不可重建

- `recording_gaps` 全库 **0 行**，但 38 个 session metadata 出现过 `recording_gap_seen`；只有布尔值，没有 gap_from/gap_to、原因、sidecar 或恢复点。
- 没有 `RECORDING_PROCESS_EXITED` 事件；restart 由 supervisor 推断。刷新模式顶层结果的 `ffmpeg_return_code` 和 `recording_state_path` 为 null，只能再解析 `stream_attempts`/sidecar。
- primary/secondary probe 的每次结果、耗时、错误分类和 room ID 没有持久化；只能看到 monitor_targets 最后状态。
- `OFFLINE_REJECTED_MEDIA_GROWTH` payload 仅有 `observed_at`，没有 baseline/current bytes、PID、mtime 和 probe 结果；重复事件占近期事件 95.75%，淹没有效信号。
- runtime stdout 日志为 0 字节；error traceback 没有自身时间戳，也没有轮转配置；启动时间另写在 `/tmp`，不耐重启/清理。

建议持久化受限保留期的 monitor observation、process exit、refresh attempt、gap open/close；事件含字节、mtime、PID、sidecar、两探针状态/版本/延迟。对重复拒绝做状态去重和计数聚合。日志统一 JSON、UTC、轮转，并对 URL/凭据做结构化脱敏。

## P2：失败场次没有终态，重试与孤儿媒体会长期积累

- 现有 8 个 `ENDED/UNKNOWN` 场次的 job 仍为 `WAITING_STREAM`；其中历史 restart_count 达 3–753。无最终媒体时 finalizer 反复返回，没有 `FAILED_FINAL`/quarantine 或人工告警。
- `recording_failure` 每第四次进入 600 秒退避，随后清零继续，没有总尝试/总时长上限。
- media/partial 有两个无 recording_job 引用的历史媒体目录，共约 2.66 GB，当前 retention 无法通过 session/job 血缘处理。
- 当前磁盘仍有约 591 GB 可用，高于 50 GB 安全线，暂非即时容量事故。

建议为“已结束但无可验证媒体”设置显式终态、dead letter/人工复核和保留策略；重试按场次总预算及错误类型封顶。增加 orphan inventory，不自动删除，先建立来源/哈希并人工处置。

## P2：配置、共享依赖与凭据暴露面

- 配置宣称 normal recording concurrency=30、expected=50、capacity test=65，但 [`v3_worker.py:389`](/Users/mac/.hermes/profiles/edu_live_competitor_intel/runtime/v3/v3_worker.py:389) 和 631 实际统一使用 `atomic_activation.max_concurrent_recordings=65`；30/50 只出现在自检/heartbeat，不是运行上限。
- profile 依赖可变的共享 recorder 和共享 DouyinLiveRecorder checkout；其 `config` 还是指向另一 profile 数据目录的 symlink。
- 共享 config（权限 0644）含一个非空、约 2143 字符的抖音 cookie；vendored `spider.py` 也含内嵌 cookie 默认值。未输出任何值。
- 持久化 sidecar/recorder.log 未发现原始 signed URL，安全指纹设计有效；但 FFmpeg 的进程 argv 包含完整签名 URL，本机进程查看者/进程采集器可读取。

建议让 normal concurrency 成为真实执行上限，容量压测单独显式开启；共享依赖按 release hash 部署。Cookie 移入权限受控的 secret store 并轮换已暴露值，删除源码默认凭据；避免采集/持久化 FFmpeg argv，若威胁模型要求隐藏 argv 中 URL，应通过受控本地 relay/FD 等方案隔离。

## 已确认有效的防护

- 当前近期 sidecar 全部为 FLV；132/132 均有 `url_sha256` 和 `resolved_at`，132 个 URL hash 全部唯一。
- sidecar 和 recorder.log 未发现原始签名 query 值；query key、query hash、URL hash可追踪。
- 文件与 sidecar 共同占用名称，失败/零字节尝试不会覆盖既有证据；sidecar 使用临时文件替换并每 5 秒快照；stderr 有 50 行上限。
- recorder 拒绝覆盖 completed session 与既有输出。
- 下游 `v3_pipeline_worker.py:118-124` 会阻止 `PARTIAL` 或 `continuous_capture!=true` 的整场媒体进入正式转录，这一门禁应保持。
- 连续三次离线与 300 秒窗口的方向正确；问题在增长 veto 的基线处理及证据持久化，而非需要缩短确认窗口。

## 验收边界

本审计没有试听/观看全部媒体，不能把 sidecar 盲区全部断言为实际内容缺失；可以确认这些区间本地没有 FFmpeg 采集进程，因此必须作为潜在 gap 进入完整性规则。本审计也没有修改历史起止时间或 COMPLETE 标签。当前“5 场 restart_count=0”只证明没有 supervisor 级重启，不能证明无刷新交接缺口、无 FFmpeg 数据错误或整场完整。
