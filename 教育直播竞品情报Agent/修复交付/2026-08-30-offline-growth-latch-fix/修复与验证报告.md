# 下播增长基线锁存：修复与验证报告

日期：2026-08-30  
范围：只修复“同一笔最终尾部字节被无限重复当成新增长”；未修改room ID拆场、900秒刷新、HLS、转录、分析、验收脚本、数据库schema或历史场次起止时间。

## 结论

状态：**已部署，回归与平滑切换验证PASS；真实离线尾部场景继续随自然直播观察。**

## 修改

- `v3_worker.py:update_session_liveness()` 每次都保存最新字节基线。
- 同一笔增长最多否决一次下播。
- 只有job为RUNNING/STARTING且PID存活时，真实新增长才允许否决离线。
- recorder已退出后的最终flush只写一条 `OFFLINE_MEDIA_TAIL_CONSUMED`，随后进入原有三次离线/300秒确认窗口。
- 事件新增 previous/observed bytes、PID和job status，便于审计。

## 测试

- 新增：活跃录制增长只否决一次，第二次无新增长即开始离线窗口。
- 新增：死进程最终尾部不阻止自然结束，300秒后转ENDED。
- Profile Python完整回归：107/107 PASS。
- LaunchAgent服务Python完整回归：107/107 PASS。

## 生产平滑切换

- 切换前Runtime PID：87059；切换后：71442；旧进程正常退出码0。
- 只向Runtime父进程发送SIGTERM；未向recorder进程或进程组发信号。
- 两个当时活跃的recorder PID 55036、55980在切换后保持原PID存活。
- 20秒观察内对应媒体继续增长5,505,024字节和5,242,880字节。
- Pipeline、Analysis、Evidence、Recompute、Retention PID均未变化。
- Runtime新心跳恢复READY；错误日志未新增字节。
- Retention持续 `DELETE_DISABLED`。
- 重载前472个媒体文件没有缺失。

## 边界

- 部署瞬间没有“PID为空、目标离线、actual bytes大于health bytes”的自然生产候选，所以不能伪称已经在真实离线尾部场景通过。
- 随后一场旧recorder在目标仍LIVE时自然退出，Runtime按既有逻辑重新启动同场recorder；这不是离线尾部验收，也没有人工改状态。
- 确定性状态机测试已覆盖原故障，未来自然出现对应场景时继续只读确认事件和终态。

## 证据

- `修改前哈希.json`
- `修改前只读基线.json`
- `offline-growth-latch-fix.patch`
- `修改后待部署哈希.json`
- `回归测试-待部署.json`
- `平滑重载前基线.json`
- `平滑重载验证.json`
- 备份：`/Volumes/ExternalStorage/AgentInfrastructure/backups/edu-offline-growth-latch-fix-20260830/`
