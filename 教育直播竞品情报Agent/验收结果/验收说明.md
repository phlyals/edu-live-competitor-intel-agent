# 验收脚本交付记录

检查日期：2026-08-28，时区 Asia/Shanghai。JSON 内 `checked_at` 使用 UTC。

## 已完成

- 三个可单独运行的只读 Python 入口已交付，见 `../验收脚本/README.md`。
- Python 3.12.13 + psycopg2-binary 2.9.12 测试：**55 项通过、0 失败、0 错误、0 跳过**。包括临时 PostgreSQL、真实 FFprobe、三入口的独立进程运行、标准表/V3 两种存储布局、5% 边界、重叠与缺口、时间戳缺失、旧报告格式、版本冲突及禁止数据库写入。
- `测试环境/测试摘要.json` 保存运行记录；其中三个 `fixture_check_*.json` 为**合成 10 秒 WAV**的成功结果，带 `test_fixture=true`。媒体、转录、报告均为测试夹具，临时源文件已删除，不是真实直播验收。
- 对真实场次运行了三个脚本，结果在本目录。没有执行补录、重转录、重新分析或修改任何生产数据库记录。

## 真实候选场次结果

场次：`session_0923b62f50088b6401c06373fc916038`。

该场次在现有数据库中标记为 `MEDIA_COMPLETE / COMPLETE`，因此作为候选执行检查；**该标签不等于已知真实完整录制**。

| 检查 | 结果 | 证据 |
|---|---|---|
| 录制完整性 | FAIL | 文件存在；FFprobe 时长 4229.749333 秒（约 70.50 分钟），1146.28 MiB；数据库预期时长 21488.405 秒（约 358.14 分钟），误差约 80.32%，超过 5% |
| 转录覆盖率 | UNCOMPUTABLE | 没有 FULL_SESSION 整场转录；`coverage_rate=null`，没有伪造覆盖率或缺口 |
| 分析报告完整性 | FAIL | 未找到该场次的分析报告；`is_complete=false` |

这组输出与读取到的数据库记录及本地媒体元数据一致。**不能将它写成“真实完整场次验收通过”**。查询时生产库 FULL_SESSION 转录数量为 0；本次没有通过改表、把样本升级为整场或放宽判定来满足验收。

## 不修改业务代码的核查

`业务代码只读校验.json` 记录实际部署目录的 SHA-256 对比。业务实现文件未发现变化。对比期间一份原有部署测试文件 `test_v3_recording_transcription.py` 发生了变化，本任务没有编辑或覆盖该文件，不能宣称整个部署目录完全不变。所有本任务交付文件仅位于项目的 `验收脚本/` 与 `验收结果/`。

集成测试通过实际只读连接尝试 UPDATE，确认 PostgreSQL 拒绝写入；另对夹具数据库内容及媒体哈希进行了运行前后比较。生产运行没有尝试写操作。

## 尚未完成的交付条件

**“对一场已知完整的真实录制运行一次，并由人工确认合理”仍未完成。** 当前已运行的是未通过的真实候选，不是已知完整场次；合成测试不能替代它，自动核查也不能替代人工确认。

待指定一场实际从开播录至自然下播、转录及报告已落盘的场次后，运行 README 中三条命令，并人工确认：起止时间符合真实开播/下播、抽查录像可播放、转录缺口是否为静音、七模块引用是否确实支持结论。确认记录应另外保存，不预先填写“人工通过”。
